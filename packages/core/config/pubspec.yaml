name: core_config
description: App config, environment variables.
version: 0.0.1
homepage:

environment:
  sdk: ^3.8.1
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

  # Env
  envied: ^1.1.1

  # Navigation
  go_router: ^16.0.0

  # Providers
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

  # Build
  build_runner: ^2.5.4
  envied_generator: ^1.1.1

flutter:
